"""Step 1 — the tabular Monte-Carlo agent: a Q-table policy learned from terminal
rewards, plus its training loops (every-visit MC, exploring starts), orchestration,
and evaluation CLI. The exact, inspectable baseline the DQN is audited against."""
