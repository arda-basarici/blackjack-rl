"""Outcome + fidelity metrics. House edge measured through the engine (vs the 0.45% anchor),
and per-cell policy fidelity weighted by how often each cell actually occurs. Reported
separately, never collapsed to one number. See DESIGN.md section 7.
"""
