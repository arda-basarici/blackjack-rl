*.pyc
__pycache__/
.venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# run outputs are regenerated; keep the folder, ignore its contents
runs/*
!runs/.gitkeep

# report charts/cache are regenerated by report_generator.py / report_generator_dqn.py
report_charts/
report_charts_dqn/

# DQN training logs (tee'd terminal output) — local only, like runs/
logs/

# private working notes for the report
REPORT_NOTES.md
REPORT_NOTES_B.md
